import{render,screen}from'@testing-library/react';import{beforeEach,expect,test,vi}from'vitest';import App from'./App';beforeEach(()=>{document.cookie='auction_token=; Max-Age=0';vi.stubGlobal('fetch',vi.fn().mockResolvedValue(new Response(JSON.stringify([{id:'1',seller_id:'s',title:'Walnut Lamp',description:'Warm light',image_key:'',starting_price:100,current_price:125,highest_bidder_id:null,auction_end_at:new Date(Date.now()+86400000).toISOString(),status:'ACTIVE',created_at:new Date().toISOString()}]),{status:200,headers:{'Content-Type':'application/json'}})))});test('renders marketplace products from the API',async()=>{render(<App/>);expect(await screen.findByText('Walnut Lamp')).not.toBeNull();expect(screen.getByText(/BDT/)).not.toBeNull()});
