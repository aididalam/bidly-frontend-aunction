export type Product={id:string;seller_id:string;title:string;description:string;image_key:string;starting_price:number;current_price:number;highest_bidder_id:string|null;auction_end_at:string;status:string;created_at:string};export type Bid={id:string;bidder_id:string;amount:number;created_at:string};const base=import.meta.env.VITE_AUCTION_API_URL??'';export const token=()=>document.cookie.split('; ').find(v=>v.startsWith('auction_token='))?.split('=')[1]||'';async function req<T>(path:string,options:RequestInit={}):Promise<T>{const t=token(),r=await fetch(base+path,{...options,headers:{'Content-Type':'application/json',...(t?{Authorization:`Bearer ${t}`}:{}) ,...options.headers}});if(!r.ok){const b=await r.json().catch(()=>null)as{error?:{message?:string}}|null;throw new Error(b?.error?.message||'Request failed')}return r.status===204?undefined as T:r.json() as Promise<T>}export const api={products:()=>req<Product[]>('/api/products'),mine:()=>req<Product[]>('/api/me/products'),create:(v:object)=>req<Product>('/api/products',{method:'POST',body:JSON.stringify(v)}),cancel:(id:string)=>req<void>(`/api/products/${id}`,{method:'DELETE'}),bid:(id:string,amount:number)=>req(`/api/products/${id}/bids`,{method:'POST',body:JSON.stringify({amount})}),bids:(id:string)=>req<Bid[]>(`/api/products/${id}/bids`),sell:(id:string)=>req(`/api/products/${id}/sell`,{method:'POST'}),presign:(filename:string,content_type:string)=>req<{upload_url:string;image_key:string;image_url:string}>('/api/uploads/presigned-url',{method:'POST',body:JSON.stringify({filename,content_type})})};export function identity(){try{return JSON.parse(atob(token().split('.')[1].replace(/-/g,'+').replace(/_/g,'/')))as{sub:string;email:string}}catch{return null}}
